"""
crosslens_spec003 — Executable contracts for SPEC-003: Evidence Packet & Architecture.

Normative definitions for:
- generation_type (computed, structured, interpreted)
- determinism_level (computed, structured, interpreted, low, unknown)
- EvidencePacket — the most consumed data object in the pipeline
- AnalysisDomainJob — orchestrator → domain dispatch work order
- PostCardValidationReport — quality check results
- ConflictReport — cross-domain conflict record
- DecisionCandidate — the final decision proposal
"""
